import React from 'react';

const ResaleCategoryCard = ({resaleProduct}) => {
    const {name,img,location,resale_price,original_price,use,posted,seller} = resaleProduct;
    return (
        <div>
            
        </div>
    );
};

export default ResaleCategoryCard;